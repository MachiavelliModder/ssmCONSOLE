_G.GA = {

	_spec = {
		IDENTIFIER = "{GW}";
		[2] = {
			timer = 0;
			text = "Sometimes our admin detectors fail, it happens! Keep your guard up.";
			color = "light_red";
		};
	};

	_norm = {
		IDENTIFIER = "{DGA}";
		[2] = {
			timer = 3;
			text = "ssm_CONSOLE isnt released to the public, so as a Tester don't leak any copy without granted permission.";
			color = "yellow";
		};
	};

};
