_G.GA = {

	_spec = {
		IDENTIFIER = "{GW}";
		[2] = {
			timer = 0;
			text = "Don't use Print() or Warn() in your code, as it can trigger exploit detection through unknown means. We are working on a patch for this issue.";
			color = "light_red";
		};
	};

	_norm = {
		IDENTIFIER = "{DGA}";
		[2] = {
			timer = 3;
			text = "ssm_CONSOLE isnt released to the public, so as a Tester don't leak any copy without granted permission.";
			color = "yellow";
		};
	};

};
