_G.GA = {

	_spec = {
		IDENTIFIER = "{GW}";
		[2] = {
			timer = 0;
			text = "ssmCONSOLE is currently undergoing extremely unstable behavior for unknown reasons. Don't try to reinstall or uninstall, as it will break the console.";
			color = "light_red";
		};
	};

	_norm = {
		IDENTIFIER = "{DGA}";
		[2] = {
			timer = 3;
			text = "ssm_CONSOLE isnt released to the public, so as a Tester don't leak any copy without granted permission.";
			color = "yellow";
		};
	};

};
